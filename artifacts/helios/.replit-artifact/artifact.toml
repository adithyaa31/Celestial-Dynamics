kind = "web"
previewPath = "/"
title = "PROJECT HELIOS"
version = "1.0.0"
id = "artifacts/helios"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19544

[services.development]
run = "pnpm --filter @workspace/helios run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/helios", "run", "build" ]
publicDir = "artifacts/helios/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19544"
BASE_PATH = "/"
